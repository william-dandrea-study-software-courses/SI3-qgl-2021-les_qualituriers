package fr.unice.polytech.si3.qgl.qualituriers.entity.boat.turnboat;

public class DispositionTest {

    /*@Test
    public void testGetter() {
        Disposition disposition = new Disposition(1, 2);
        assertEquals(1, disposition.getBabordOar());
        assertEquals(2, disposition.getTribordOar());
    }

    @Test
    public void testGetterDistance() {
        DistanceDisposition disposition = new DistanceDisposition(1, 2, new Point(3, 4), 5);
        assertEquals(1, disposition.getBabordOar());
        assertEquals(2, disposition.getTribordOar());
        assertEquals(new Point(3,4), disposition.getFinalPositionOnTheMap());
        assertEquals(5, disposition.getDistanceToCheckPoint());
    }

    @Test
    public void testEqualsDistance() {
        DistanceDisposition disposition = new DistanceDisposition(1, 2, new Point(3, 4), 5);
        assertEquals(new DistanceDisposition(1, 2, new Point(3, 4), 5), disposition);
        assertNotEquals(new DistanceDisposition(1, 2, new Point(3, 4), 6), disposition);
        assertNotEquals(new DistanceDisposition(1, 2, new Point(5, 8), 5), disposition);
        assertNotEquals(new DistanceDisposition(1, 4, new Point(3, 4), 5), disposition);
        assertNotEquals(new DistanceDisposition(2, 2, new Point(3, 4), 5), disposition);
        assertNotEquals(disposition, null);
        assertNotEquals(disposition, "test");
    }

    @Test
    public void testHashCodeDistance() {
        DistanceDisposition disposition = new DistanceDisposition(1, 2, new Point(3, 4), 5);
        assertEquals(new DistanceDisposition(1, 2, new Point(3, 4), 5).hashCode(), disposition.hashCode());
        assertNotEquals(new DistanceDisposition(1, 2, new Point(3, 4), 6).hashCode(), disposition.hashCode());
        assertNotEquals(new DistanceDisposition(1, 2, new Point(5, 8), 5).hashCode(), disposition.hashCode());
        assertNotEquals(new DistanceDisposition(1, 4, new Point(3, 4), 5).hashCode(), disposition.hashCode());
        assertNotEquals(new DistanceDisposition(2, 2, new Point(3, 4), 5).hashCode(), disposition.hashCode());
    }*/

}
